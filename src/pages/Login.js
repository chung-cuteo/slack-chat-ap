import styled from "styled-components";

const Login = () => {
  return (
    <LoginContainer>
      <LoginInnerContainer>aaaa</LoginInnerContainer>
    </LoginContainer>
  );
};

export default Login;

const LoginContainer = styled.div``;

const LoginInnerContainer = styled.div``;
